import { DebounceHoverDirective } from './debounce-hover.directive';

describe('DebounceHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceHoverDirective();
    expect(directive).toBeTruthy();
  });
});
